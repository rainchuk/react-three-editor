import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  objects: {
    width: "100%",
    height: 300,
    borderRadius: 8,
    background: "#fff",
    border: "1px solid #f0f0f0"
  }
});
